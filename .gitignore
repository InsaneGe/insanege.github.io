# .gitignore 文件是一个文本文件，用于告诉 Git 版本控制系统哪些文件或目录应该被忽略，即不被添加到仓库中。这个文件通常位于项目的根目录下。通过使用 .gitignore 文件，你可以避免不必要的文件被提交到仓库中，从而保持仓库的整洁和高效。
# Bundler cache
.bundle
vendor
Gemfile.lock

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/dist
assets/js/dist
